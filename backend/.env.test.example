# Test Database Configuration
TEST_DATABASE_URL="mysql://username:password@localhost:3306/ticket_system_test"
DATABASE_URL="mysql://username:password@localhost:3306/ticket_system_test"

# JWT Configuration (can use simpler values for tests)
JWT_SECRET="test-jwt-secret-key-for-testing-only"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_SECRET="test-refresh-secret-key-for-testing"
JWT_REFRESH_EXPIRES_IN="24h"

# Server Configuration
PORT=3002
NODE_ENV="test"

# Email Configuration (use test values)
SMTP_HOST="smtp.ethereal.email"
SMTP_PORT=587
SMTP_USER="test@ethereal.email"
SMTP_PASS="test-password"
EMAIL_FROM="test@ticketsystem.com"

# Security Configuration
BCRYPT_ROUNDS=4
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# CORS Configuration
FRONTEND_URL="http://localhost:3000"